kind: pipeline
name: default

steps:
- name: build
  image: pandry/scienzabotb
  commands:
  - mkdir -p $GOPATH/src/scienzabot/
  - cp -r * $GOPATH/src/scienzabot/
  - go get
  - go build .
- name: static analysis
  image: pandry/scienzabotb
  commands:
  - mkdir -p $GOPATH/src/scienzabot/
  - cp -r * $GOPATH/src/scienzabot/
  - go get
  - golint .
  - go vet .
  - maligned ./...
