kind: pipeline
name: default

steps:
- name: build
  image: pandry/scienzabotb
  commands:
  - pwd
  - ls -la
  - cp -r * $GOPATH/src/scienzabot/
  - go get
  - go build .
- name: static analysis
  image: pandry/scienzabotb
  commands:
  - cp -r * $GOPATH/src/scienzabot/
  - go get
  - golint .
  - go vet .
  - maligned ./...
